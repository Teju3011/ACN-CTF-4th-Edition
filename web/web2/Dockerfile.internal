FROM node:20-alpine

# Install wget for health checks
RUN apk add --no-cache wget

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy internal service code
COPY internal-service.js ./

# Expose port
EXPOSE 5001

# Start the internal service
CMD ["node", "internal-service.js"]
